CREATE TABLE LOG (ID INTEGER NOT NULL, LEVEL VARCHAR(255), ANNÉE INTEGER, HEURE INTEGER, JOUR INTEGER, MILLISECONDES INTEGER, MINUTES INTEGER, MOIS INTEGER, SECONDES INTEGER, MACHINE_ID INTEGER, PRIMARY KEY (ID))
CREATE TABLE MACHINE (ID INTEGER NOT NULL, NAME VARCHAR(255), PRIMARY KEY (ID))
CREATE TABLE MACHINE_LOG (Machine_ID INTEGER NOT NULL, logs_ID INTEGER NOT NULL, PRIMARY KEY (Machine_ID, logs_ID))
ALTER TABLE LOG ADD CONSTRAINT FK_LOG_MACHINE_ID FOREIGN KEY (MACHINE_ID) REFERENCES MACHINE (ID)
ALTER TABLE MACHINE_LOG ADD CONSTRAINT MACHINE_LOGlogs_ID FOREIGN KEY (logs_ID) REFERENCES LOG (ID)
ALTER TABLE MACHINE_LOG ADD CONSTRAINT MCHINELOGMachineID FOREIGN KEY (Machine_ID) REFERENCES MACHINE (ID)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT DECIMAL(15), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 0)
